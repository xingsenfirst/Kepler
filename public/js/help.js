/** 应用内使用帮助文档 */
import { openModal } from './util.js';

const CONTENT = `
<div class="help-sec">
    <h4 id="s1">重要声明</h4>
  <ul>  
    <li>使用此软件，需使用者已熟悉对象存储相关操作，无相关经验者请勿使用。</li>
    <li>受部分服务商限制，未设回收站功能。删除无法找回。</li>
    <li>除非另行声明，该系统可随意操作修改存储桶，任何操作均请三思。</li>
    <li>仅用于对象存储，因此无法提供本地挂载和离线下载功能。</li>
  </ul>
  <h4 id="s2">快速开始</h4>
  <ul>
    <li>支持阿里云、腾讯云、华为云、又拍云、七牛云、Microsoft Azure、AWS S3，输入 API Keys 之前需选择正确的服务商。</li>
    <li>需安装 Node.JS 且建议版本 ≥18。</li>
    <li>启动本地服务：右键选择“在此打开 PowerShell 窗口”，然后执行 <code>npm start</code>，再使用浏览器打开 <code>http://localhost:3000</code>。</li>
    <li>单文件执行：直接运行 build.exe，程序会自动打开浏览器窗口。</li>
    <li>严禁使用主账号 API，务必仅使用只具有对象存储相关权限子账号的访问密钥（AccessKey / SecretKey 等，各服务商叫法不同）。</li>
  </ul>

  <h4 id="s3">安全说明</h4>
  <ul>
    <li>服务默认仅监听 127.0.0.1，不对局域网开放；如需开放请设置环境变量 <code>HOST=0.0.0.0</code> 并自行评估安全风险。</li>
    <li>受对象存储原生功能影响，本程序不设回收站，删除即为永久丢失。</li>
    <li>所有访问密钥经本地 AES-256-GCM 加密，永不回传浏览器，设置页仅显示掩码。</li>
    <li>为防止中间人攻击截获访问密钥，服务器端部署将强制使用 HTTPS 协议，本地执行无此限制。</li>
    <li>上传排除：可选择排除 .DS_Store 文件、排除 Thumbs.db 文件或根据 .gitignore 规则排除文件。开启对应规则后，上传流程将在文件选择阶段与上传处理阶段自动过滤命中的文件，它们不会进入上传队列、不会上传到云端。选项保存后即时生效并持久化，重新登录后仍然保持。与 GitHub Push 规则一致：.gitignore 文件同样会被上传，虽然它通常极小，但这仍然会产生流量费用。</li>
    <li>经加密的文件仅限本系统中拥有权限的用户可预览和下载，对象存储控制台中将显示为密文，无法读取。</li>
    <li>遗忘管理员密码，前往 data 文件夹下删除 config.enc 文件即可，此操作将会重置整个管理系统，需重新绑定访问密钥和所有个性化设置，<b>并会丢失所有文件的加密密钥</b>。</li>
    <li>除非特别声明，后述机能问题不影响基本的预览、上传、下载等功能，但可能会卡顿：单次上传超过 5000 个对象可能会导致 API 读取异常导致无法正确显示已上传和正在上传的对象数量；单次上传超过 20000 个对象可能会导致系统卡顿；绑定超过 5000 个存储桶可能会导致无法正确读取缓存；绑定超过 300000 个 Access Key 可能会导致 UI 渲染崩溃；分享链接超过 500000 个可能会导致 UI 渲染崩溃。</li>
  </ul>

  <h3><br>能力越大，责任越大。数据无价，谨慎操作。</h3>
</div>`;

export const help = {
  init() {},
  open() {
    const m = openModal({ title: '使用帮助', wide: true, body: CONTENT, foot: [{ text: '关闭' }] });
  },
};
